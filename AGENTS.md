# Akastr Agent — 仓库约束

按任务从 [README.md](README.md) 定位实现、权威文档和验证入口，不默认通读整个 docs。

## 1. 产品与安全边界

- Agent 是 systemd 管理的单 Go 进程，负责本机受控执行、观察和恢复；Cloud 负责业务与跨节点编排。保持轻量、低资源、少依赖，性能优化不增加不必要的复杂度。
- 只执行本机预配置、协议允许的 typed operation，主动建立出站控制连接；不提供 HTTP 控制面、远程 shell、任意 argv/script/URL 或开放式主机控制。
- secret 只在 Git 外 root-only 文件中保存，不进入日志、测试 fixture、capability 或普通 wire payload；仅允许协议明确需要的受控 credential 流程。
- 本地持久状态必须有界，无法证明安全恢复时 fail closed，不静默重置可能涉及副作用的状态。具体幂等、互斥和恢复语义见 [ARCHITECTURE.md](docs/ARCHITECTURE.md)，wire 以 [PROTOCOL.md](docs/PROTOCOL.md) 为准。
- 未实现功能不创建空实现、占位 wire 字段或未被真实需求使用的框架。

## 2. 跨仓库与批准边界

- 职责归属和跨仓库验证以 Cloud `docs/AGENT_INTEGRATION.md` 为准；共享契约变化核对双方，纯内部变化只修改承担相应职责的仓库。双方审查不等于双方都要修改；本机仓库可通过 `$local-project-paths` 定位。
- schema、持久契约、认证/secret、安全边界或发布模型的高风险变化，按 Cloud ADR 0024 在实施前批准具体方案，扩大范围重新批准；代码注释、ADR 或 AI 推断不能创造批准。
- 长期跨仓库决定统一记录在 Cloud `docs/ADR/`，不建立第二套 ADR 注册表。

## 3. 文档与验证

行为或契约变化同步更新唯一权威说明，清理相关旧描述。文档职责、维护细则和按变更类型执行的验证要求统一见 [README.md](README.md#文档维护) 与 [本地开发](README.md#本地开发)。

## 4. 工程偏好

- 偏好成熟、满足当前需求的简单设计，优先复用合适的现有实现，避免无必要的兼容层、临时补丁、启发式兜底和预防性抽象。
- 避免为低概率故障过度防御，复杂度应与实际风险相称。
- 保持结构清晰、模块化、易维护；package 职责与架构一致，新增 feature 优先采用职责清晰的同级 package。
- 架构面向长期，不以以后重写为前提。

## 5. 回复偏好

- 用户没有编程和运维基础。回复使用大白话，不假设用户懂技术术语；必要术语随用随解释。先说结果及对用户的实际影响，有实际风险时说明；需要用户操作时，给出可直接照做的步骤，不省略关键前提。
